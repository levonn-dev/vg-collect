meta {
  name: get self
  type: http
  seq: 1
}

get {
  url: {{user_url}}/users/{{user_id}}
  body: none
  auth: inherit
}

docs {
  Run auth/dev-token first to populate access_token and user_id.
  Expect 200 with the alice fixture's profile and roles.
}
