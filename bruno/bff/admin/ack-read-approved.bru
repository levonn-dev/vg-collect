meta {
  name: ack - alice reads the approved submission
  type: http
  seq: 56
}

get {
  url: {{bff_url}}/api/entries/{{ack_entry_id}}/submission
  body: none
  auth: none
}

assert {
  res.status: eq 200
  res.body.status: eq approved
  res.body.resolution_ack_at: isUndefined
}
