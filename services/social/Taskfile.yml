version: "3"

tasks:

  default:
    desc: List available tasks (run with no args)
    silent: true
    cmds:
      - task --list

  gen:
    desc: Regenerate server stubs from api/social.yaml
    cmds:
      - go tool oapi-codegen -config ../../api/oapi.server.yaml ../../api/social.yaml

  db:migrate:
    desc: Run migrations against DATABASE_URL
    cmds:
      - go run ./cmd/social migrate
