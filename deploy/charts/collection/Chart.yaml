apiVersion: v2
name: collection
description: vgkeep collection service (entries, tags, views, dashboard) with its Postgres and Valkey
type: application
version: 0.1.0
appVersion: "0.1.0"
dependencies:
  - name: vg-lib
    version: 0.1.0
    repository: file://../vg-lib
