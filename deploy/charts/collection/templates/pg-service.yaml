{{- if .Values.postgres.enabled }}
apiVersion: v1
kind: Service
metadata:
  name: collection-pg
  labels: {{- include "collection.labels" . | nindent 4 }}
spec:
  selector:
    app.kubernetes.io/name: collection-pg
  ports:
    - { name: pg, port: 5432, targetPort: pg }
{{- end }}
