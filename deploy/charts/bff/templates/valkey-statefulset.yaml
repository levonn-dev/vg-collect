# Cache semantics: no persistence. Denylist entries live at most
# accessTokenTtl + leeway, so losing them on a restart is the
# accepted fail-open posture, not data loss.
{{- include "vg-lib.valkey.statefulset" . }}
