apiVersion: v2
name: enrichment
description: vgkeep enrichment service (catalog + pricing) with its MongoDB and Valkey
type: application
version: 0.1.0
appVersion: "0.1.0"
dependencies:
  - name: vg-lib
    version: 0.1.0
    repository: file://../vg-lib
