apiVersion: policy/v1
kind: PodDisruptionBudget
metadata:
  name: enrichment
  labels: {{- include "enrichment.labels" . | nindent 4 }}
spec:
  minAvailable: 1
  selector:
    matchLabels:
      app.kubernetes.io/name: enrichment
