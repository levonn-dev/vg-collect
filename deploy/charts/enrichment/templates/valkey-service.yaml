{{- if .Values.valkey.enabled }}
apiVersion: v1
kind: Service
metadata:
  name: enrichment-valkey
  labels: {{- include "enrichment.labels" . | nindent 4 }}
spec:
  selector:
    app.kubernetes.io/name: enrichment-valkey
  ports:
    - { name: valkey, port: 6379, targetPort: valkey }
{{- end }}
