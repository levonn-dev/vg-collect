{{- if .Values.postgres.enabled }}
apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: social-pg
  labels: {{- include "social.labels" . | nindent 4 }}
spec:
  selector:
    matchLabels:
      app.kubernetes.io/name: social-pg
  endpoints:
    - port: metrics
      interval: 30s
{{- end }}
