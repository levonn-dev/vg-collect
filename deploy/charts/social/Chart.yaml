apiVersion: v2
name: social
description: vgkeep social service (follows, likes, comments, activity) with its Postgres
type: application
version: 0.1.0
appVersion: "0.1.0"
dependencies:
  - name: vg-lib
    version: 0.1.0
    repository: file://../vg-lib
