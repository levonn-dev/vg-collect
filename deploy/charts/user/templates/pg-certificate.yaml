{{- if .Values.postgres.enabled }}
apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: user-pg-tls
  labels: {{- include "user.labels" . | nindent 4 }}
spec:
  secretName: user-pg-tls
  duration: 2160h
  dnsNames:
    - {{ include "user.pgHost" . }}
    - {{ include "user.pgHost" . }}.{{ .Release.Namespace }}.svc
    - {{ include "user.pgHost" . }}.{{ .Release.Namespace }}.svc.cluster.local
  issuerRef:
    name: vg-ca
    kind: ClusterIssuer
    group: cert-manager.io
{{- end }}
