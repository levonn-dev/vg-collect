{{- if .Values.postgres.enabled }}
apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: auth-pg-tls
  labels: {{- include "auth.labels" . | nindent 4 }}
spec:
  secretName: auth-pg-tls
  duration: 2160h
  dnsNames:
    - {{ include "auth.pgHost" . }}
    - {{ include "auth.pgHost" . }}.{{ .Release.Namespace }}.svc
    - {{ include "auth.pgHost" . }}.{{ .Release.Namespace }}.svc.cluster.local
  issuerRef:
    name: vg-ca
    kind: ClusterIssuer
    group: cert-manager.io
{{- end }}
