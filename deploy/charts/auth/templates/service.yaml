apiVersion: v1
kind: Service
metadata:
  name: auth
  labels: {{- include "auth.labels" . | nindent 4 }}
spec:
  selector:
    app.kubernetes.io/name: auth
  ports:
    - { name: http, port: 8080, targetPort: http }
