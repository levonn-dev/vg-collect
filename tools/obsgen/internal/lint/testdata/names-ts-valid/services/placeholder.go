// Package services exists only so this fixture's services/ directory
// is real and non-empty (Known errors on a missing scan root) and git
// tracks it; it registers nothing on purpose - this fixture's whole
// point is the frontend/src TypeScript pass below, not the Go one.
package services
