# Minimal valid alerts/golden.yaml; Load always attempts it regardless of
# which manifest kind this fixture is actually exercising.
group:
  name: vgkeep
  folder: vgkeep
  interval: 1m
datasource: prometheus
services: []
external_metric_prefixes: [kube_, node_, up]
templates: {}
