# Fixture: a bogus key inside a service alerts file's rule list must fail
# Load, distinct from the same check in cluster.yaml (a different file kind
# even though both use the Rule shape).
service: alpha
golden: {}
alerts:
  - uid: vg-alpha-queue-backlog
    title: Alpha queue not draining
    expr: max(vg_alpha_queue_pending)
    condition: gt 25
    instant: true
    for: 10m
    noDataState: OK
    execErrState: Error
    severity: warn
    summary: the alpha queue has stayed above 25 pending
    runbook: alpha.md#queue-backlog
    bogus_field: oops
