# Minimal valid golden.yaml so Load reaches dashboards/alpha.yaml.
group:
  name: vgkeep
  folder: vgkeep
  interval: 1m
datasource: prometheus
services: [alpha]
external_metric_prefixes: [kube_, node_, up]
templates: {}
