# Filler: this tree's case is about a different file; a bare dashboard
# entry with no custom panels keeps dashboards/alpha.yaml out of the way so
# Load fails for exactly one reason.
service: alpha
dashboard:
  uid: vg-alpha
  title: Alpha
custom_panels: []
