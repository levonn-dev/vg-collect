# Fixture: shares uid vg-dup-thing with bravo.yaml's rule below, on purpose.
service: alpha
golden: {}
alerts:
  - uid: vg-dup-thing
    title: Alpha copy of a duplicated rule
    expr: max(vg_alpha_thing)
    condition: gt 1
    instant: true
    for: 5m
    noDataState: OK
    execErrState: Error
    severity: warn
    summary: alpha side of an accidental duplicate uid
    runbook: alpha.md#dup-thing
