# Fixture: the panel text is valid yaml (a plain block scalar) but not
# valid json, so Load's block validation must fail with this file's path.
blocks:
  availability:
    panels:
      - |
        { this is not valid json
