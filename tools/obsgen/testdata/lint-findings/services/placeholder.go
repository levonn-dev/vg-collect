// Package services exists only so this fixture's services/ directory
// is real (Known errors on a missing scan root) and git tracks it.
// This fixture's one rule and one panel reference only "up" (covered
// by external_metric_prefixes), so no metric registration is needed
// here.
package services
